{
  "name": "gameserver-client",
  "version": "1.0.0",
  "description": "the client for gameserver",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Peter Elliott",
  "license": "LGPL-3.0-or-later",
  "devDependencies": {
    "typescript": "^4.8.4"
  },
  "dependencies": {
  }
}
